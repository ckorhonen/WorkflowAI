# Agent is deployed on WorkflowAI in production
# Agent logs runs with metadata including user_id
# user_id "usr_12345" reported bad experience with agent "agent_abc123"

# Example production agent with metadata tracking
user_id = "usr_12345"
agent_id = "agent_abc123"

# Need to investigate this specific user's experience
